/**
 * Mockup-aligned import path. Canonical menu edits: `lib/menu-data.ts`.
 */
export {
  menuCategories,
  menuCategories as menu,
  popularPicks,
  POPULAR_PHOTO_URLS,
  type BoardMenuItem,
  type BoardMenuCategory,
  type PopularPickCard,
} from "../lib/menu-data";
